name: Sample proogram for dispatch event

on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: set up python
      uses: actions/setup-python@v2
      with:
        python-version: 3.8

    - name: install toml 
      run: pip install toml

    - name: run the code
      run: |
        echo "runs the code"


